# Metacritical Homebrew tap.
Personal HomeBrew Tap

Install Tiny scheme using brew through this tap.

```sh
$ brew tap metacritical/tap
$ brew install <formula name> #Just the formula name without the .rb extension
```
